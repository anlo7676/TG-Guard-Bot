# 旧机器人清理与验证流程复查

用户确认清理 8429656729、8934664978 的本地运行数据，保留当前 7958769155。

已完成：
- 删除最初项目旧库 tgguard（5 条旧收件箱记录）及 tgguard_8934664978（180 条旧收件箱记录）。其他项目数据库不在清理范围。
- 核实当前 Redis DB 1 绑定 7958769155 后，删除 DB 0 中 20 个旧 ai／keyword／spam／web 键，未使用 FLUSHALL。
- 删除 tmp 下原 Git 备份及旧切换／诊断／数据库检查目录、旧 Linux 构建和旧备用二进制。活动 .git、.env、当前程序与用户上传的需求资料保留。
- 配置已迁入当前数据库，属于当前机器人的有效配置，不随旧库删除。

同类流程修复：
- 验证成功后提示清理／成功私聊通知失败可持久重试。
- 终态通知恢复不再执行解禁或欢迎。
- 已通过验证的答案重放与旧链接不会误报答案错误。
- 旧终态记录迁移时标记通知完成，不批量补发历史群消息。

验证：全量 Go 测试（独立 MySQL 测试库、Redis 集成）、go vet、6 项前端测试通过。新增测试模拟提示删除和成功通知失败、恢复后去重及重复提交。
